# TODO: Fix JWT Duplicate Declaration & Refactor Auth - COMPLETE

## Steps:
- [x] Step 1: Update backend/server.js (remove duplicates, add imports, use connectDB, add authRoutes)
- [x] Step 2: Create backend/models/User.js (User schema)
- [x] Step 3: Create backend/controllers/authController.js (signup/login logic)
- [x] Step 4: Create backend/routes/authRoutes.js (auth routes)
- [x] Step 5: Install deps if needed & test server

**Next:** Run `cd backend` then `npm install jsonwebtoken bcryptjs` (if not installed) and `node server.js` to verify no SyntaxError.
